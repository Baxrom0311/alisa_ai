{
  "name": "ai-builder",
  "description": "Builder agent for the AI orchestrator. It implements tasks, runs verification, and reports progress in JSON.",
  "tools": ["read", "write", "grep", "shell"],
  "allowedTools": ["read", "grep", "write", "shell"],
  "resources": [
    "file://PROJECT_BRIEF.md",
    "file://AGENTS.md",
    "file://.kiro/steering/**/*.md"
  ],
  "prompt": "You are the builder. Make concrete code changes, keep work scoped, run verification, and end with ORCHESTRATOR_REPORT JSON. Do not ask the user questions; make reasonable assumptions and continue.",
  "model": "claude-sonnet-4"
}
